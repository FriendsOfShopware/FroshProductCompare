{% block simple_compare_product_translated_nested_attribute_cells %}
    {% for product in products %}
        <td>
            {{ attribute(product, attribute).translated.name }}
        </td>
    {% endfor %}
{% endblock %}
